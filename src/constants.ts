/**
 * Global application constants and feature flags
 */

// Feature Flags
export const FEATURES = {
  /**
   * Enable markdown rendering in agent execution viewer
   * Set to true to render markdown in agent text and reasoning blocks
   */
  ENABLE_MARKDOWN_RENDERING: false, // Temporarily disabled - set to true to enable
} as const;

// Other global constants can be added here
